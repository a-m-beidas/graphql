schema {
    query: Query
}

type Query {
    bestSellers: [Book]
    recent: [Book]
    classic: [Book]
}

type Book {
    id: String
    title: String
    genre: String
    author: String
    link: String
    year: Int
}